soccer:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('PGHOST', 'localhost') }}"
      port: "{{ env_var('PGPORT', '5432') | int }}"
      user: "{{ env_var('PGUSER', 'soccer') }}"
      password: "{{ env_var('PGPASSWORD', 'soccer') }}"
      dbname: "{{ env_var('PGDATABASE', 'soccer') }}"
      sslmode: "{{ env_var('PGSSLMODE', 'prefer') }}"
      schema: public
      threads: 4
